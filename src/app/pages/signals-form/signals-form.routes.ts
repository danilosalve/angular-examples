import { Routes } from '@angular/router';

import { SignalsFormComponent } from './signals-form.component';

export const SIGNALS_FORM_ROUTES: Routes = [
  {
    path: '',
    component: SignalsFormComponent,
  },
];
